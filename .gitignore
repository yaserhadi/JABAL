/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
env.workspace
branch_audit_*.json

# .cursor/ is KSS + agent workspace (memory, goals, rules, commands copy targets).
# Do NOT blanket-ignore it: that hides GOALS/memory from search tools and breaks /boot.

# GitHub workflows (if local only)
.github/

# Specstory
.specstory/
